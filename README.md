Clone my respository [here](https://github.com/MartaAS/product-management)


Install the dependencies:

```npm install```


To start the application:
Launching the app is done in the root  of the project.

```npm start```

